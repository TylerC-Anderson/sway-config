# Input
input type:keyboard xkb_numlock enabled

input type:pointer {
    accel_profile "flat"
    pointer_accel 0.15
}

# Focus behavior
focus_follows_mouse no
mouse_warping none
# Focus wraps at the workspace edge, matching the AeroSpace behavior on the Mac.
focus_wrapping workspace
